{
  "name": "actuary-math-practice",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run sync:data && next dev",
    "build": "npm run sync:data && next build",
    "sync:data": "npm run sync:problems && npm run sync:formulas",
    "sync:problems": "node scripts/sync-problems.mjs",
    "sync:formulas": "node scripts/sync-formulas.mjs",
    "start": "next start"
  },
  "dependencies": {
    "next": "^14.2.16",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.6.0"
  }
}
